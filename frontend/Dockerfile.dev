# Development Dockerfile for Frontend
FROM node:18-alpine

WORKDIR /app

# Install dependencies are handled by volume mount
# Just copy everything and let npm install run as part of the command

# Expose port
EXPOSE 3000

# Install dependencies and start dev server
CMD ["sh", "-c", "npm install && npm run dev -- --host 0.0.0.0 --port 3000"]